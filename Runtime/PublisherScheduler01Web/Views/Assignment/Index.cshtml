@model IEnumerable<PublisherScheduler01Web.ViewModels.AssignmentViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SlotSelected)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TaskTypeSelected)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PersonSelected)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SlotSelected)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TaskTypeSelected)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PersonSelected)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssignmentDetail.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssignmentDetail.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssignmentDetail.Id })
        </td>
    </tr>
}

</table>
